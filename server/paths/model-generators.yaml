---
get:
  tags:
  - Model Generators
  summary: Get list of model generator names
  operationId: list_model_generators

  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/list_model_generators.yaml
    401:
      $ref: ../components/responses/unauthorized_error.yaml
    403:
      $ref: ../components/responses/forbidden_error.yaml
